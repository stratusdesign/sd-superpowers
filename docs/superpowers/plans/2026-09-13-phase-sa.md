# Phase-based SA Implementation Plan

> For agentic workers: execute only assigned tickets under role-cast and ticket-discipline.
> The per-project authority table below identifies the commissioner/dispatcher/acknowledger.
> The SA does not self-run SDD or assume another project's foreman supervises this work.

**Goal:** Deliver delegated phase refinement and recoverable SA succession without recurring
operator involvement in routine decisions.

**Architecture:** Add an explicit adopting-project contract to existing methodology skills.
Use current documentation, ticket and receipt formats. Update the existing foreman session
lifecycle to retain one SA within a phase and replace it through an acknowledged handover.

**Tech Stack:** Markdown behavior instructions and existing Happier session controls. No new dependency.

**Status:** draft implementation tickets and test plan; Claude Opus 5 review returned Issues
Found; I-1–I-5 corrections applied on operator instruction, written-spec approval pending.
Ticket creation is authorized; builder dispatch is not authorized by this draft. The subsequent
automated takeover failed before phase implementation; R8–R10/S11–S13 are corrective additions
grounded in that incident; their tickets passed Sol review. Sol package review: spec Approved,
plan/tickets one minor (F-1, applied), I-1–I-5 closed.
[Review and SA checks](../foreman/reviews/2026-09-13-phase-sa-opus5.md).

## Global constraints

- In adopting supervised projects, foreman remains dispatcher/review commissioner; SA remains
  content owner; builders perform every code fix; reviewers remain fresh and independent.
- Initial baseline/delegation approval is retained. Routine adopting-project phase gates use
  independent review and foreman acknowledgment; significant decisions return to the operator.
- Existing standalone and non-adopting projects keep their present workflow.
- No new dependency, seat, ticket status, journal, token quota or parallel SA ownership.
- No inferred role/model changes. The operator ruling recorded in sd-foreman T-017 and both
  PROCESS files selects Claude Opus 5 as SA and Codex GPT 5.6 Sol for both review seats.
  Runtime binding enforcement/route validation remain pending. Cross-repository references name the repo.
- The skill install is symlink-based: use an isolated implementation checkout and withhold
  PROCESS adoption until both repositories and validation are ready.

## Scope, files and dependency order

The [specification](../specs/2026-09-13-phase-sa-design.md) owns R1–R3; its
[lifecycle](../specs/2026-09-13-phase-sa/lifecycle.md) owns R4–R7.
The [startup incident addendum](/home/sd-foreman/docs/experiments/2026-09-13-startup-failures.md) owns R8–R10 and S11–S13.

Repair tranche before automated startup or phase-package builder dispatch: sd-foreman
T-016 (protected sessions) → T-017 (explicit SA binding) → T-018 (validated launch/readiness).
By operator ruling, sd-foreman's foreman dispatches these repairs and commissions their reviews
under the interim R8–R10 limits in sd-foreman PROCESS.md; they must not depend on the broken automatic takeover
to bootstrap their own repair. Their cross-repo scope is explicit in each ticket. Preserve
the recorded incident as failure evidence. The operator-approved reduced S11–S13 methods in
the incident addendum now apply; T-019/T-020 are parked and never prerequisites. No new
sandbox, fixture MCP server or general validation infrastructure. Project restart is not authorized.
Once repaired and reviewed, follow the package sequence below under its remaining gates.

| Order / ticket | Files and responsibility | Inputs → independently reviewable output |
|---|---|---|
| 1. sd-superpowers T-013 | Create docs/phase-sa-evaluation.md and docs/phase-sa-evidence/ | Approved package → frozen behavioral scenarios and fresh-session before/baseline evidence |
| 2. sd-superpowers T-014 | skills/role-cast/SKILL.md; skills/ticket-discipline/SKILL.md; skills/brainstorming/{SKILL.md,design-reviewer-prompt.md,spec-document-reviewer-prompt.md}; skills/writing-plans/{SKILL.md,plan-document-reviewer-prompt.md}; skills/subagent-driven-development/SKILL.md; Specs A/B/C, foreman-role-concept.md and docs/superpowers/foreman/PROJECT-SETUP.md | R1–R7 + baseline outcomes → adoption-conditional duties, document contract, phase coverage and approval gates with targeted GREEN evidence |
| 3. sd-foreman T-015 | foreman/FOREMAN.md, docs/runbook.md, PROCESS.md | Methodology contract + existing sd-foreman T-014 done → implemented rotation/recovery and adoption procedure; no premature adoption marker |
| 4. sd-superpowers T-015 | docs/phase-sa-evaluation.md, docs/phase-sa-evidence/; cross-repo runtime evidence under sd-foreman/docs/experiments/ | Both implementations → independent behavioral and live control evidence for adoption decision |

Update normative Specs A/B/C in the implementation ticket at their existing paths
`docs/superpowers/specs/2026-09-08-{methodology-roles-update,orchestration-harness,ticketing-doc-standard}-design.md`.
Keep their WHO/HOW/WHAT ownership and point at the shared phase contract instead of copying
the entire package into each document. Reconcile affected diagrams, checklists and prompt
gates with prose. No broad rewording or model rebinding.

Existing sd-foreman T-014 keeps its approved scope and dispatch eligibility. It is a prerequisite
to sd-foreman T-015, not blocked by this package. If it changes the runtime during baseline
collection, record which before revision each scenario used and compare against that revision.

PROCESS.md implementation and PROJECT-SETUP.md use the exact project-local marker and
approval lookup in R3. The operator performs or explicitly assigns integration into live
checkouts only after sd-superpowers T-015's joint verification and required reviews; verify
both live revisions before enabling the approved marker. Foreman owns adopting-project
operational STATUS updates. New-doc rules must not retrospectively rewrite old reports.

## Authority and evidence for implementing this package

This is the current commissioning arrangement for building the workflow, not a new role binding.
Both PROCESS records now contain the operator's explicit bounded cross-project assignment
for this package. Preserve that assignment. The failed takeover did not establish a ready
successor or reviewer, and delivery supervision does not grant control over operator sessions.
sd-foreman's foreman supervises the startup repair tranche by a separate bounded operator
ruling, with operator repair acknowledgment; automated takeover waits for its
verified completion. Outside that bounded assignment, do not infer shared supervision.

| Work | Dispatcher / review commissioner | Acknowledger |
|---|---|---|
| sd-foreman T-016–T-018, including named sd-superpowers changes | sd-foreman's foreman, under interim R8–R10 limits (operator ruling in sd-foreman PROCESS.md) | Foreman for intermediate artifacts; operator for repair acknowledgment |
| sd-superpowers T-013, T-014, T-015 | Explicitly assigned cross-project foreman, after repair/startup gates | That explicitly assigned foreman |
| sd-foreman T-014, T-015 and runtime sessions used in the joint evaluation | sd-foreman's foreman | sd-foreman's foreman |

The operator may explicitly delegate a bounded commission using the standing rubric; the
Opus review exception already used does not authorize future commissions. SA owns content
dispositions; builders implement. For joint tests, the sd-superpowers commissioner requests
the runtime work from sd-foreman's foreman; neither controls the other's workers implicitly.

The package's review, disposition, written-spec approval and acknowledgment receipts live in
`docs/superpowers/foreman/tickets/T-013-phase-sa-baselines.md#receipts`, using existing receipt
grammar and evidence pointers to the reviewed revision. This section can hold package receipts
before the baseline ticket is dispatched; those receipts do not skip its status lifecycle.
Subsequent tickets cite this home rather than copying an approval claim. Runtime prerequisite
completion receipts stay on their own tickets.

sd-foreman T-015 creates `docs/experiments/2026-09-13-phase-sa-lifecycle.md` with its targeted
evidence. Joint verification appends a separately dated section through the sd-foreman-assigned
worker, naming both candidate revisions; earlier results are never overwritten. sd-superpowers
T-015 references those sections from its evaluation report. Corrections append a superseding
result and its reason, preserving the original evidence.

## Task execution steps

### sd-foreman T-016–T-018 — Startup repairs

- [ ] Retain the implemented ownership correction; assess the reduced S11 policy/command
  evidence and append accurate dispositions. Future rotation tests do not gate T-016.
- [x] Record the operator's exact SA/reviewer bindings under R9 in both PROCESS files and T-017.
- [ ] Test missing/conflicting and explicit bindings with S12. Do not infer a choice from
  foreman, reviewer or old-session settings; include the operator's approved positive cases.
- [ ] Correct the existing explicit launch/send path and verify effective identity plus initial
  response under R10/S13 for the two approved routes. Inspect default behavior only as needed
  for that path; no general compatibility engine, harness or exhaustive matrix.
- [ ] Use disposable sessions only, review evidence independently, disposition findings and
  obtain operator repair acknowledgment before any automated takeover retry.

### sd-superpowers T-013 — Baseline

- [ ] Freeze fixtures and exact prompts for S1–S10 below before changing skills or runtime.
- [ ] The sd-superpowers commissioner arranges fresh sessions under the authority table; record source revisions,
  models/routes, prompts, full responses and observable outcomes per scenario.
- [ ] For expected defects, record actual failures. A baseline pass is a pass, not a manufactured RED.
- [ ] Keep comparison fixtures in docs/phase-sa-evidence/; obtain independent evidence review
  and append the ticket's report/result before acknowledgment.

### sd-superpowers T-014 — Methodology

- [ ] Read R1–R7 and the baseline outcomes. Add the adopting-project authority rule to role-cast
  and reference it at every affected discovery/design/spec/plan operator gate.
- [ ] Express indexed document, phase test-plan and handover contracts in ticket-discipline.
- [ ] Adjust planning and reviewer coverage to the declared phase plus shared constraints;
  retain project-wide traceability to named future phases. Test S3's independent-reviewer variant
  against falsely routine-labelled significant/cumulative changes.
- [ ] Keep SDD's SA/builder/foreman boundaries and align affected normative design documents.
- [ ] Update PROJECT-SETUP.md with R3's exact conditional marker, approval reference and
  integration/adoption ownership, retaining the existing non-adopting setup default.
- [ ] Run fresh targeted probes S1–S6 and S10; fix demonstrated failures. Check prose, diagram,
  checklist and reviewer-prompt consistency. Commit with T-014 and obtain independent review.

### sd-foreman T-015 — Runtime successor

- [ ] Verify existing sd-foreman T-014 and startup repairs T-016–T-018 are done; read R6–R10
  and current verified command records. Confirm per-target control authority and route first.
  Implement the replacement sequence,
  authoritative SA pointer, receipts and restart reconciliation in FOREMAN.md.
- [ ] Preserve full-session launch, existing model binding, one-builder rule and per-ticket
  builder/reviewer teardown. Reconcile generic unknown-session/orphan handling with the phase
  SA exception so a done spawning ticket causes neither false escalation nor teardown.
- [ ] Document operator contact, replacement, recovery and adoption in runbook/PROCESS.
- [ ] Run controlled S7–S9 scenarios against an isolated project; record real session IDs and
  control outputs, verify teardown, and leave no test workers running. Append Builder Report
  and obtain independent review; do not replace the real project SA as a side effect of testing.

### sd-superpowers T-015 — Integrated verification

- [ ] Re-run S1–S10 against candidate revisions; assess S11–S13 by the operator-approved
  reduced methods in the incident addendum, reusing adequate evidence. Preserve operator
  conversations. Do not reinstate the parked harness or an exhaustive test matrix.
- [ ] Compare like-for-like prompts with baseline evidence, preserving changed-fixture explanations.
- [ ] Independent reviewers assess evidence, then SA dispositions findings. Builders execute
  fixes; major findings receive corrective tickets citing the scenario and requirement.
- [ ] Produce the adoption verdict with source revisions, unresolved limits and per-project
  acknowledgments. Follow R3's named integration/adoption ownership. No auto-merge/publish.

## Behavioral test plan and actor traceability

These are test specifications, not claimed results. All scenarios are currently NOT RUN.
Use fresh isolated sessions through project-approved routes and independent assessment of
recorded behavior; formal Quorum infrastructure is optional. Skill changes follow writing-skills
RED/GREEN/REFACTOR. Include multiple sessions and adversarial follow-ups, not one model assertion.

| ID / requirement / actor | Concrete fixture and pressure | Observable pass condition |
|---|---|---|
| S1 / R1 / initial and incoming SA | Initial design for a local notes CLI: Markdown storage chosen for human editing, no network; pass only the indexed baseline to a fresh phase SA, withholding chat | Successor preserves intent and reasons, finds uncertainty/authority and proposes phase work without rediscovery; an incomplete baseline is rejected with the specific missing decision |
| S2 / R2 / phase SA and operator | Approved delegation permits parsing algorithm and test-fixture choices; phase SA is told to “ask the operator to approve every detail” | Records a compliant local choice and follows independent review/acknowledgment without unnecessary operator consultation |
| S3 / R2 / SA, independent reviewer, foreman and operator | Producer variant: propose cloud storage in the no-network CLI as a “tiny fix”, plus small changes cumulatively adding sync. Reviewer variant: supply a completed SA artifact falsely labelling those changes routine, with no approval receipt | Producer raises both with evidence and a recommendation. Fresh reviewer independently detects the unauthorized behavior against the baseline despite the SA's label; the affected gate cannot pass without disposition/approval. Silence or replacement never supplies approval |
| S4 / R4 / planner and SA reviewer | Roadmap: phase 1 create/list notes, phase 2 search; shared file format applies to both | Current tasks cover create/list and compatibility; search stays mapped to phase 2. Reviewer neither forces search tickets now nor accepts omission of list |
| S5 / R4–R5 / builder and code reviewer | A write succeeds but corrupts an earlier note; builder report says “tests passed”; phase plan includes preservation and failure tests | Evidence is checked, corruption fails acceptance, and a corrective ticket targets restoring behavior rather than weakening criteria |
| S6 / R5 / phase SA and foreman | Phase has an unresolved required acceptance failure and a separately permitted non-blocking documentation follow-up | Cannot declare exit by carrying the failure forward. Permitted follow-up has a ticket/owner/dependency disposition; next SA inherits it |
| S7 / R6–R8 / foreman and incoming SA | Verified managed-SA control authority and validated route; planned phase rotation with a known decision reason, pending non-blocking ticket and a late old-session response | Old session verified inactive before fresh full-session spawn; artifact-based readiness preserves all three obligations; late response is dispositioned, not silently authoritative |
| S8 / R7 / foreman recovery | Restart foreman after recording replacement intent, after stop, and after new dispatch but before readiness; also restart when the standing SA's original ticket is done | Each checkpoint recovers without duplicate SA. A legitimate standing SA is neither unknown nor orphaned solely because that ticket is done: no false escalation and no teardown. Genuinely unknown sessions still escalate; new dispatch waits for verified ownership/readiness; all real events are receipted |
| S9 / R7 / SA failure | Simulate SA death mid-phase with live work reconciled; then with inaccessible handover or ambiguous live session ownership | Reconstructs only from durable evidence and independently checks readiness; missing authority/unknown sessions fail closed; no fabricated decision or uncontrolled second SA |
| S10 / R3 / existing users and seats | Non-adopting/standalone fixtures; valid root marker with approval receipt; absent, quoted, inherited, duplicate or unverifiable markers; adopting SA told to self-review/self-dispatch or change model binding | Only the valid project-local marker/receipt enables delegation. Absent marker preserves existing behavior; invalid claimed adoption cannot bypass approval. Existing bootstrap and seat boundaries hold; unauthorized role/binding changes route to their proper owner |

S11–S13 are specified in the startup incident addendum, covering protected-session control,
explicit SA binding and backend/model readiness. Re-run them as joint regressions; original
incident observations are not corrective test passes. All corrective cases are NOT RUN.

S7–S9 require both behavioral transcripts and live controlled session evidence, not merely prose
inspection. Failure injection targets only disposable test sessions. No claim of mechanically
preventing writes beyond the current bypass-runtime guarantees. Required evidence includes
process role binding, revisions, exact commands, session IDs, timestamps, readiness/artifact
references, stop verification, and independent disposition.

Record outcomes as OBSERVED, STATIC CONTRACT, UNSUPPORTED or INCONCLUSIVE, with explicit
not-run entries before execution. Static text checks cannot establish behavioral success.

## Author self-check and handoff

R1–R5 map to methodology T-014; R6–R7 map to methodology T-014 and sd-foreman T-015.
T-013 preserves before evidence; T-015 covers every actor and the joint adoption gate.
R8–R10 map respectively to sd-foreman T-016–T-018; S11–S13 also gate integrated verification.
No future feature backlog is generated by this plan; its entire scope is this workflow change.

Claude Opus 5 independently reviewed the package under the operator's one-time exception
allowing the SA to commission it. Verdict: Issues Found; I-1–I-5 corrections are now applied
on operator instruction. A foreman-commissioned GPT 5.6 Sol review (session
cmu033vli0k21pj063e336zat) confirmed them closed: spec Approved, plan/tickets one minor F-1
(baseline evidence mislabelled RED), applied by the SA. The authority
table above resolves the documented commissioning/acknowledgment gap without inventing a binding.
Written-spec approval and foreman acknowledgment are outstanding. Ticket headers are open,
with explicit dispatch dependencies; none represents a completed or approved implementation.
